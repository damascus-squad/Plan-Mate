package org.damascus.logic.usecase.project

import logic.repo.ProjectRepository
import java.util.*

class GetProjectStateUseCase(private val repository: ProjectRepository) {
    operator fun invoke(projectId: UUID) {

    }
}